const path = require('path');

module.exports ={
    // inline: false,
    // contentBase: path.join(__dirname, "dist"),
    // index:'index.html',
    port:3002,
    hot:true,
    historyApiFallback:true
    // writeToDisk: true 
}